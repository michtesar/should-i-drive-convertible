import React from "react"
import {Stack, Typography} from "@mui/material";

export const AppHeadings = () => {
    return (
        <Stack margin={'5%'}>
            <Typography variant={'h3'}>Should I lower the roof?</Typography>
        </Stack>
    )
}